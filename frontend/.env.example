# Optional: API base URL. If unset, Next.js rewrites proxy /auth, /config, etc. to http://localhost:8080.
# Set this if your API runs elsewhere (e.g. http://localhost:8080 or https://api.example.com).
# NEXT_PUBLIC_API_BASE=
